# CryptoCustode

## Agent skills

### Issue tracker

Issue e spec vivono come issue GitLab su `gitlab.trecuori.org`, gestite con la CLI `glab`.
See `docs/agents/issue-tracker.md`.

### Triage labels

Le cinque label canoniche, con i nomi di default (`needs-triage`, `needs-info`,
`ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` e `docs/adr/` alla radice del repo.
See `docs/agents/domain.md`.
